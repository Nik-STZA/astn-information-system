{
  "name": "stza-finance-api",
  "version": "0.1.0",
  "private": true,
  "description": "Finance module API. Reads shared.* and finance.* only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "runner": "node runner.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@google-cloud/secret-manager": "^6.3.0",
    "express": "^4.21.0",
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
