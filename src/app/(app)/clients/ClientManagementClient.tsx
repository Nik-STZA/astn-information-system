/**
 * DEPRECATED — this file is orphaned and no longer imported.
 * Retained only to avoid build errors until the next cleanup pass
 * deletes it from git. The active client management UI is in
 * ClientsClient.tsx.
 */
export {};
